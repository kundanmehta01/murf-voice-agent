# Minimal dependencies to avoid Rust compilation
# All of these have pre-compiled wheels available

fastapi==0.85.0
uvicorn==0.18.3
python-dotenv==1.0.0
requests==2.31.0
jinja2==3.1.2
pydantic==1.10.12
websockets==11.0.3

# Try to skip problematic packages for now
# We'll add them back one by one if deployment succeeds
